6. Close On Escape: in read-only mode only, configurable through the INI file (off by default)

3. Support Tab Completion For Active REPL Filter:
   - Triggered by pressing TAB while on the Prompt Line after the detected prompt
   - Send the input from inputStart to the caret position via WriteFile on g_hREPLStdin,
     WITHOUT appending a newline/EOL
   - Set g_bREPLTabCompletionPending = TRUE and start a short timeout timer (~2-3 s)
   - On the next WM_REPL_OUTPUT (first stdout chunk after the Tab send), call
     ReplaceREPLInput instead of InsertREPLOutput: detect input start via DetectPrompt,
     select from inputStart to end-of-line, EM_REPLACESEL with the completion text
     (strip any trailing newline from the response)
   - If the timeout fires before any response, clear the flag silently
   - Subprocess cooperation required: the REPL filter must read stdin without waiting
     for newline (e.g. WScript.StdIn.Read(1) in JScript, sys.stdin.read(1) in Python),
     detect that input ended with \t rather than \n, write back the completed text and
     flush stdout; standard shells behind a pipe will not work
   - RichEditor side: ~60-80 lines (Tab intercept, send, flag+timer, ReplaceREPLInput)
   - Cannot be tested/debugged without a live Windows build

1. Search & Replace, bookmarks
* add edit field to the Find dialog box to show found text and context for more accessible reviewing of found text and ability to continue search with the open dialog

5. Markdown:
   - Parser: https://github.com/mity/md4c
   - Markdown to RTF converter: https://github.com/iquercorb/md4c-rtf
